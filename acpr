# Trier les variables de la moins importante à la plus importante
fi_df_least = fi_df.sort_values("importance", ascending=True)

# Afficher les 20 moins importantes
print("\n🔻 Les 20 variables les moins importantes :")
print(fi_df_least.head(20))

# Tracer les 20 moins importantes
k = 20
fi_least = fi_df_least.head(k).iloc[::-1]  # Pour affichage visuel de bas en haut
plt.figure(figsize=(8, 6))
plt.barh(fi_least["feature"], fi_least["importance"])
plt.xlabel("Importance (CatBoost)")
plt.ylabel("Variables")
plt.title(f"Variables les moins importantes – CatBoost (Bottom {k})")
plt.tight_layout()
plt.show()
